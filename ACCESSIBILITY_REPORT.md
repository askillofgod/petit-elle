# ACCESSIBILITY_REPORT.md — 접근성 감사

- 작성일: 2026-06-07 · 기준: WCAG 2.1 AA 지향

---

## 1. 이미지 대체 텍스트 (alt) — ✅ 양호
- 모든 `next/image` 에 의미 있는 한국어 `alt` 부여(Hero/브랜드/프로그램 카드/공간 갤러리/시그니처/예약 썸네일 등, 총 11곳).
- 예: "Petit Elle 프라이빗 힐링 공간에서 휴식하는 여성 고객", "Petit Elle 리셉션 로비".
- 권장: 순수 장식 이미지가 추가될 경우 `alt=""` 명시.

## 2. aria-label / 시맨틱 — ✅ 양호
- 아이콘 전용 버튼에 `aria-label` 부여(헤더 햄버거, Top 버튼, 푸터 SNS, 캘린더 이전/다음 달, 관리자 메뉴, 공지 노출/삭제 등 11곳).
- 아코디언: `<button aria-expanded>` + 콘텐츠 토글(키보드 동작).
- 시맨틱: `header/main/footer/nav/section`, 페이지당 `h1` 1개 + `h2` 섹션 구조 준수.
- `<html lang="ko">` 설정.

## 3. 키보드 내비게이션 — ✅ 양호
- 모든 인터랙션이 네이티브 `<button>`/`<a>`(Link) 기반 → Tab/Enter/Space 동작.
- 포커스 표시: 버튼 `focus-visible:ring-2 ring-ring`, 인풋 `focus:ring-1 focus:border-gold`.
- 예약 캘린더 날짜/시간 슬롯, 스테퍼, 관리자 액션 모두 키보드 접근 가능.
- 권장: 모바일 드로어 오픈 시 포커스 트랩(선택), `Esc` 닫기(선택).

## 4. 색 대비 (Contrast) — ⚠️ 주의 항목 있음
| 조합 | 용도 | 추정 대비 | AA(4.5/3.0) |
|---|---|---|---|
| brown `#6A5A52` / ivory `#F7F3EF` | 제목/본문 | ~6.7:1 | ✅ |
| ink `#2E2926` / white | 본문 | ~13:1 | ✅ |
| muted `#7A6D66` / ivory | 보조 텍스트 | ~4.7:1 | ✅(경계) |
| **white / gold `#B88D7A`** | **Primary 버튼 텍스트** | **~2.7:1** | ❌ |
| **gold `#B88D7A` / white** | **가격·링크 강조 텍스트** | **~2.7:1** | ❌(소형 텍스트) |

- **핵심 발견:** 브랜드 포인트인 샴페인 골드(#B88D7A)는 흰 배경/흰 텍스트와의 대비가 약 2.7:1로 **소형 텍스트 AA(4.5) 미달**.
- 버튼 텍스트는 16px medium 이상이라 체감은 양호하나 엄밀 기준 미달.

### 권장(택1)
1. **접근성 우선:** 텍스트를 품는 면(Primary 버튼/소형 골드 텍스트)에 한해 더 진한 톤 사용
   - 예: 버튼 배경을 `#9A6F5B`~`#8A6450` 수준으로(현 hover `gold-dark #A87A67`보다 진하게) → 4.5:1 근접.
   - 골드 소형 텍스트는 `brown`으로 대체하거나 크기/굵기 상향.
2. **브랜드 우선(현행 유지):** 디자인 시스템 톤 유지 + 본 항목을 "의도된 브랜드 편차"로 명시(대형/굵은 텍스트에 한정 사용).
- 결정 필요 사항으로 OPEN_ITEMS / DECISIONS 에 반영 권장.

## 5. 기타
- 모션: fade-in/up 위주(절제), 과도한 모션 없음. `prefers-reduced-motion` 대응은 미적용(권장 추가).
- 폼: label 연결(`<Label htmlFor>` / id) 적용, 에러 텍스트 시각 표시(스크린리더용 `aria-describedby`는 미연결 — 권장).
- 터치 타겟: 버튼 높이 ≥44px(디자인 시스템 준수).

## 종합
| 항목 | 상태 |
|---|---|
| alt | ✅ |
| aria-label/시맨틱 | ✅ |
| 키보드 | ✅ |
| 대비 | ⚠️ (골드 텍스트면 AA 미달 — 결정 필요) |
| 모션/폼 보조 | ⚠️ (reduced-motion, aria-describedby 권장) |
