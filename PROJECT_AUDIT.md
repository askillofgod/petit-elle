# PROJECT_AUDIT.md — Petit Elle 전체 감사

- 작성일: 2026-06-07 · 상태: Mock 기반 V1, Cloudflare Workers(OpenNext) 배포 구성 완료

---

## 1. 현재 구현된 기능 (UI + Mock 동작)

### 공개(고객)
- 홈(9섹션: Hero/브랜드/시그니처/프로그램/Why/공간갤러리/CTA/FAQ/오시는길)
- 프로그램 목록 + 상세(SSG, 4종), 공간 소개, FAQ, 오시는 길, 로그인(UI), 약관/개인정보, 404/error
- **예약 플로우 5단계**(zod 검증 + 서버 액션 + 완료 페이지)
- 마이페이지(예약 내역/상세/내 정보), 플로팅 예약 버튼/Top 버튼

### 관리자
- 대시보드, 예약관리(검색/필터/상태 액션), 예약 상세, 일정관리(슬롯/휴무), 고객관리(검색/정렬), 프로그램관리(정렬/CRUD/노출), 공지사항, 설정, 로그인(UI)

### 공통/인프라
- 디자인 시스템(Tailwind 토큰), 반응형(360~1440+), next/image+sharp
- **Service Layer 6종 + Server Actions + zod 검증**
- SEO(metadata/sitemap/robots/JSON-LD), OpenNext 배포 구성

---

## 2. 현재 Mock 기능 (실제 영속성 없음)

| 영역 | Mock 소스 | 비고 |
|---|---|---|
| 프로그램 | `lib/mock/programs.mock` | CRUD는 globalThis 메모리 |
| 예약 | `lib/mock/reservations.mock` | 생성/상태변경 메모리 반영 |
| 고객 | `lib/mock/customers.mock` | 집계 뷰(예약횟수 등) 고정 |
| 슬롯 | `lib/mock/slots.mock` | 생성/휴무 메모리 |
| 공지/설정/관리자 | `lib/mock/*` | 설정 저장은 더미 토스트 |

> ⚠️ Mock 변이는 `globalThis` 보관(개발 프로세스 한정, 서버 재시작 시 초기화). 영속성은 DB가 담당.

---

## 3. 실제 DB(Supabase) 연결이 필요한 기능
- 예약 영속 저장/조회, 슬롯 재고·동시성(중복 예약 방지)
- 회원(카카오 OAuth) / 관리자(이메일) 인증 + 세션 + `/admin` 보호
- 고객 집계(예약횟수/최근방문) 실데이터, 마이페이지 사용자별 필터
- 프로그램/공지/설정 영속 변경, 활동 로그
- (가이드: `SUPABASE_SETUP_GUIDE.md`, 스키마: `supabase/schema.sql|seed.sql|rls.sql`)

---

## 4. 지금 "오픈 가능한" 것 (브로셔/시연 수준)
- 브랜드 홈페이지 + 프로그램/공간/FAQ/오시는길 (정적 콘텐츠) — **실서비스 가능**
- 예약 플로우 **시연**(접수 화면까지) — 단, 실제 예약 저장/알림은 없음
- 관리자 화면 **시연/내부 데모**

## 5. 아직 "오픈 불가능한" 것
- 실제 예약 접수·확정(영속/알림 없음) → 고객에게 "예약됨" 보장 불가
- 회원 로그인/마이페이지(인증 없음)
- 관리자 실운영(인증·권한 없음 → 보안상 공개 불가)

---

## 6. 위험 요소
| 위험 | 영향 | 완화 |
|---|---|---|
| 관리자 미인증 | `/admin` 공개 노출 시 데이터 조작 가능(현재 Mock이라 피해는 제한) | 연동 시 `middleware` 필수, 그 전 공개 배포 시 `/admin` 비노출 |
| Mock 영속성 오해 | 예약이 저장된 것으로 오인 | 완료 화면 문구로 "신청 접수" 명시(되어 있음) |
| 임시 사업정보 | 잘못된 주소/번호 노출 | OPEN_ITEMS.md 실데이터 교체 |
| 예약 동시성 | 연동 후 중복 예약 | RPC 트랜잭션 + 슬롯 카운트 |
| CDN 폰트 의존 | 오프라인/차단 시 폰트 폴백 | 시스템 폰트 폴백 지정됨(허용 가능) |

---

## 7. 기술 부채
- 설정 폼: 저장 액션 미배선(더미 토스트) → controlled + `updateSettingsAction`
- 고객 취소 요청 버튼 미배선, 고객 상세 페이지 없음
- 프로그램 이미지 업로드 없음(썸네일 고정), slug 자동 생성 단순
- Service Layer 일부 메서드 미연결(연동 대비 스캐폴딩 — 의도적, REFACTOR_REPORT 참고)
- 시간 슬롯 가용성: 예약 플로우는 결정적 Mock, 관리자 슬롯과 미연동
- 마이페이지 사용자 `c1` 고정

---

## 8. 품질 게이트 (2026-06-07)
- `pnpm typecheck` ✅ 0 · `pnpm lint` ✅ 0 · `pnpm build` ✅ (30/30) · `pnpm cf:build`(OpenNext) ✅ · `preview`(워커) CSS/라우트 ✅
